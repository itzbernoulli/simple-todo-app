import React from "react"

const Header = () => {
    return(
        <header>
            <h1>Simple Todo App</h1>
            <p>Please add to_dos item(s) through the input field</p>
        </header>
    )
}
export default Header